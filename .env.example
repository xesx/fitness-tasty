# Copy to .env and fill in real values. .env is gitignored — see CLAUDE.md.

# Shared Postgres database used by all apps/backend-* services (packages/prisma).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fitness_tasty?schema=public"
